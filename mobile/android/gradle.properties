org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Contournement d'un échec reproductible de Gradle 9.1.0 (MergeInstrumentationAnalysisTransform :
# "Could not deserialize analysis from a file") apparu dès l'ajout des plugins natifs
# record/just_audio/permission_handler — persiste après un cache complètement vidé
# (~/.gradle/caches), donc pas une corruption locale mais un vrai bug de cette version.
org.gradle.configuration-cache=false
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
